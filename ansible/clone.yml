---
- name: Clone repo from Express42 reddit app git
  hosts: app-stage
  tasks:
    - name: Clone repo
      ansible.builtin.git:
        repo: https://github.com/express42/reddit.git
        dest: /home/ubuntu/reddit2
